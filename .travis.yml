language: go
os:
  - linux
  - osx
go:
  - 1.15.x
script: make build
matrix:
  allow_failures:
    - go: tip
git:
  submodules: false
deploy:
  provider: releases
  api_key: ${GITHUB_API_KEY}
  file: bin
  skip_cleanup: true
  on:
    tags: true